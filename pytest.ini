[pytest]
# Keep bare pytest inside the tracked suite. The Wine prefix under state/ has a
# dosdevices/z: symlink to /, so unbounded discovery can escape the repository.
testpaths =
    tests
norecursedirs =
    state
